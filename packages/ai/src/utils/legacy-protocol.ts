/** External service identities. These URLs and client ids are not product branding. */
export const RADIUS_GATEWAY_ORIGIN = "https://radius.pi.dev";
export const RADIUS_OAUTH_CLIENT_ID = "pi-gateway";
