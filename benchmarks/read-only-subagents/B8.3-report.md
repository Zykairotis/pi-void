# B8.3 Comparative Report

Status: **abbreviated sanity comparison completed — 2/4 rows completed, 2 timed out**

Read-Only Subagents v1 implementation is **frozen** by the deterministic B8.1 and B8.2 gates; this B8.3 report provides abbreviated external comparison evidence only.

## Baselines

- `ice-stock`: `e47b8e37a6211ebd0b2942fa87059d64f81eec02`
- `ice-native-example`: `e47b8e37a6211ebd0b2942fa87059d64f81eec02`
- `ice-subagents`: `67cf559acbb4b621b53879e2df3c8bd211c2b44b`
- `ice`: resolved from the current workspace at run time

The external checkouts matched their manifest SHAs exactly. The harness performed no fetch, checkout, branch switch, dependency upgrade, or target configuration rewrite during execution.

## Provider Route Repair

The prior 160-row report used ICE's custom `local` provider path. Those rows are retained as historical setup diagnostics only; they provide no comparable scenario evidence and must not be used to freeze v1.

The harness now uses the upstream-compatible `openai` provider with the local OpenAI-compatible endpoint. `manifest.json` versions the exact endpoint metadata for both requested models:

- `cx/gpt-5.6-luna`: `openai-responses`, context `272000`, maximum output `128000`, reasoning enabled, text input
- `cmc/deepseek/deepseek-v4-pro`: `openai-completions`, context `1000000`, maximum output `384000`, reasoning enabled, text input

Each target receives a temporary route-only `models.json`. The credential is inherited from `ICE_LOCAL_API_KEY`; it is not copied into configuration, result rows, or CLI arguments. No ICE `local` provider configuration or personal `auth.json` is copied.

## Provider Smoke

The fixed smoke prompt requests a no-tool reply and requires completed agent output from all four targets. Both model routes passed:

| Model | Targets | Result | Output evidence |
| --- | ---: | --- | --- |
| `cx/gpt-5.6-luna` | 4 | `4/4` completed | `18–19` assistant-output bytes per target |
| `cmc/deepseek/deepseek-v4-pro` | 4 | `4/4` completed | `18–19` assistant-output bytes per target |

The smoke result artifacts are local ignored JSONL files. They record provider/model route, resolved commit, completion status, output byte count, and bounded usage metrics; they do not contain credentials or transcripts.

The credential preflight was also regression-tested. When `ICE_LOCAL_API_KEY` is absent, the harness emits:

```text
benchmark provider credential ICE_LOCAL_API_KEY unavailable
```

before target preparation or process spawn.

## Abbreviated Sanity Benchmark

This was a **4-row sanity comparison, not the full B8 matrix**. It used the exact requested command and route:

```bash
node benchmarks/read-only-subagents/src/cli.ts \
  --matrix \
  --model cx/gpt-5.6-luna \
  --scenario quality.correctness-and-evidence
```

Result artifact: `23bd2d3d-9ef0-4387-9404-ce69b7f4cff0.jsonl`

| Target | Result | Resolved commit | Wall time | Tokens in/out | Output bytes | Verification | Failure | Real assistant output |
| --- | --- | --- | ---: | ---: | ---: | --- | --- | --- |
| `ice-stock` | success | `e47b8e37a6211ebd0b2942fa87059d64f81eec02` | 118.783s | 56,273 / 4,168 | 774 | `agent_completed` | none | yes |
| `ice-native-example` | success | `e47b8e37a6211ebd0b2942fa87059d64f81eec02` | 52.172s | 48,255 / 2,109 | 1,207 | `agent_completed` | none | yes |
| `ice-subagents` | failure | `67cf559acbb4b621b53879e2df3c8bd211c2b44b` | 120.033s | 35,248 / 699 | 0 | `target_execution_failed` | `timeout` | no |
| `ice` | failure | `7cbd8a676815cbfef3b03a4902269ede2ac2fbf1` | 120.029s | 73,974 / 3,312 | 1,084 | `target_execution_failed` | `timeout` | yes, partial |

Failure classification:

- `ice-subagents`: **adapter/target limitation** — the pinned external target exceeded the fixed 120-second harness budget without observed assistant text.
- `ice`: **model-quality weakness** — the target produced assistant text but did not complete within the fixed 120-second budget; this artifact does not establish a correctness, isolation, lifecycle, budget, or verification defect.
- No actual ICE defect was found. No production runtime change was made.

Read-Only Subagents v1 implementation remains **frozen** based on the deterministic B8.1 adversarial tests and B8.2 harness/provider smoke gates. The full `48` deterministic-row and `112` model-quality-row comparative gates remain optional external certification evidence.

## Release Decision

B8.3 has **abbreviated scenario evidence** from the 4-row sanity comparison. The harness and route work, two targets completed the requested scenario, and the two timeouts are recorded as target/model execution limitations rather than setup failures or ICE defects.

Read-Only Subagents v1 implementation remains **frozen** based on the deterministic B8.1 adversarial tests and B8.2 harness/provider smoke gates. The full `48`/`112`-row comparative gate remains optional external certification evidence. This abbreviated sanity run provides no basis for additional production hardening.
